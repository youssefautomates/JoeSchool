const { createClient } = require("@supabase/supabase-js"); const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL || ""; const serviceRoleKey = process.env.SUPABASE_SERVICE_ROLE_KEY || ""; const supabaseAdmin = createClient(supabaseUrl, serviceRoleKey, { auth: { persistSession: false } }); console.log("auth constructor:", supabaseAdmin.auth.constructor.name); console.log("admin:", !!supabaseAdmin.auth.admin); if (supabaseAdmin.auth.admin) { console.log("keys of admin:", Object.keys(supabaseAdmin.auth.admin)); }
